spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fyp-grading
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin

    notification:
      queue: "notification-queue"
      exchange: "notification-exchange"
      routing-key: "notification-key"